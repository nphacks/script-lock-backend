const express = require('express');
const router = express.Router();
const axios = require('axios');

// Integrate Admin API functionality
/**
 * Automate user management
 * Must have Docusign Admin enabled on your enterprise account to use this API in production, but it is available in the development environment.
 */

// Note: The developer and production endpoints for most Docusign APIs use slightly different paths. 

module.exports = router;